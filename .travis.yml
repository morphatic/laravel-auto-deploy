language: php

php:
  - 5.6
  - 7.0

before_script:
  - composer self-update
  - composer require satooshi/php-coveralls:dev-master --prefer-source --no-interaction --dev

script: phpunit -c phpunit.xml --coverate-text

after_script:
  - php vendor/bin/coveralls -v